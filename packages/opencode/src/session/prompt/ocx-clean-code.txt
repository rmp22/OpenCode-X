=== CLEAN CODE CHECK ===

Run this after every write:
- Remove dead code, unused names, stubs, TODOs, FIXMEs, and placeholders.
- Reuse existing code. Remove forwarding methods and duplicate logic.
- Do not add an abstraction for three similar lines.
- Remove guessed setup, unnecessary wrappers, and tiny helpers that add no real boundary.
- Split files at real responsibility or change boundaries. Do not keep unrelated domains together or add one-file-per-function wrappers.
- Keep one source of truth. Regenerate artifacts from source.
- Delete scratch and backup files. Check every CTA reaches its control.
- Check that each changed path fits existing callers, settings, errors, lifetime, and tests.
- Verify changed inputs, outputs, module links, APIs, and settings against source or documentation.
- Test relevant empty, malformed, denied, failed, timed-out, cancelled, and partial states.
- Keep behavior, errors, security, compatibility, and tests.

Fix violations before delivery.

=== END CLEAN CODE CHECK ===
