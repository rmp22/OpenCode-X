# OCX Workflow Reminder

Keep each change focused. Read the target and nearby code before editing. Reuse existing code where it fits. Verify APIs from source. Check the diff and report the checks you ran.

After a write, remove dead code, duplicate logic, unused imports, unused variables, alias wrappers, stubs, TODOs, and placeholders. Keep required behavior, error handling, security checks, compatibility, and tests.
